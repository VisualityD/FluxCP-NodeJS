export function NotFoundPage() {
  return (
    <>
      <h3>Page not found</h3>
      <p>
        The React rewrite currently includes the application shell and the first migrated routes.
        Remaining FluxCP modules should be ported incrementally behind this layout.
      </p>
    </>
  );
}
